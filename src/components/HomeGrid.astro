---
const { items = [] } = Astro.props;
---
<ul class="home-grid" role="list">
  {items.map(item => (
    <li class="card" key={item.href}>
      <a href={item.href} aria-label={item.label}>
        <img src={`${item.image}?v=2`} alt={item.label + ' image'} />
        <h3>{item.label}</h3>
      </a>
    </li>
  ))}
</ul>
